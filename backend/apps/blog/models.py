"""Database models for blog app.

Will be populated in Round 2 with:
- Category (tree structure via parent FK)
- Tag
- Post (with Markdown content, status workflow, SEO metadata)
"""
from django.db import models

# Models will be added in Round 2
